/* hysterical raisons, but too small to remove */
openssh-client
/* could be used by debconf in certian configurations */
libterm-readline-gnu-perl
/* Accessibility stuff that is installed by base-installer
 * in certian situations, but too large for the netinst. */
brltty
speakup
/* See rationalle in #630805 */
apt-offline
